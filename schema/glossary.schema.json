{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://atlaschuti.cz/schema/glossary.schema.json",
  "title": "Atlas chutí – Slovníček",
  "type": "object",
  "required": ["title", "category", "short_definition"],
  "properties": {
    "locale": { "type": "string", "description": "BCP 47 jazykový tag lokalizovaného obsahu. Chybí-li, importer použije \"cs-CZ\".", "default": "cs-CZ" },
    "translation_group": { "type": "string", "description": "Stabilní, jazykově nezávislý identifikátor sdílený všemi jazykovými verzemi TOHOTO pojmu. Chybí-li, odvodí se ze slugu. Přes něj se pojem dohledává v related_glossary, ne přes slug nebo WordPress post ID." },
    "translation_status": { "type": "string", "enum": ["none", "draft", "reviewed", "published"], "description": "Stav (budoucí) lokalizace vůči originálu. Pro originál nech \"published\" (výchozí)." },
    "title": { "type": "string", "description": "Název v jazyce daném polem locale." },
    "slug": { "type": "string", "description": "URL slug pro TENTO jazyk." },
    "category": { "type": "string", "enum": ["technique", "ingredient", "gastronomy", "equipment"], "description": "Stabilní klíč, ne české jméno — např. \"technique\", ne \"Kuchařské techniky\" (viz Atlas_Chuti_Taxonomy_Labels)." },
    "short_definition": { "type": "string" },
    "detailed": { "type": "string" },
    "origin_country": { "type": "string", "description": "Stabilní identita země původu (volitelné): preferuj ISO 3166-1 kód, slug je fallback." },
    "taste": { "type": "string" },
    "usage": { "type": "string" },
    "substitute": { "type": "string" },
    "related_recipes": { "type": "array", "items": { "type": "string" }, "description": "translation_group (preferováno) nebo slug." },
    "related_countries": { "type": "array", "items": { "type": "string" }, "description": "ISO kódy (preferováno) nebo slugy." },
    "seo_title": { "type": "string" },
    "meta_description": { "type": "string" },
    "status": { "type": "string", "enum": ["publish", "draft"], "default": "publish" }
  }
}
